import React from "react";
import { render } from "@testing-library/react";

import Row, { RowProps } from "./row";
import Col, { ColProps } from "./col";

const defaultRowProps: RowProps = {};
const defaultColProps: ColProps = {};

describe("Row、Col组件测试", () => {
  it("", () => {});
});
